<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.counter.favorite.FavoriteMapper">

    <insert id="insert">
        INSERT IGNORE INTO user_favorites (id, user_id, post_id, created_at)
        VALUES (#{id}, #{userId}, #{postId}, NOW(3))
    </insert>

    <delete id="delete">
        DELETE FROM user_favorites WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="listPostIds" resultType="long">
        SELECT post_id FROM user_favorites
        WHERE user_id = #{userId}
        <if test="lastId != null">AND id &lt; #{lastId}</if>
        <if test="folderId != null">AND folder_id = #{folderId}</if>
        ORDER BY id DESC
        LIMIT #{size}
    </select>


    <update id="assignFolder">
        UPDATE user_favorites SET folder_id = #{folderId}
        WHERE user_id = #{userId} AND post_id = #{postId}
    </update>

    <update id="unassignFolder">
        UPDATE user_favorites SET folder_id = NULL
        WHERE user_id = #{userId} AND folder_id = #{folderId}
    </update>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*) FROM user_favorites WHERE user_id = #{userId}
    </select>

    <select id="countDistinctAuthors" resultType="int">
        SELECT COUNT(DISTINCT kp.creator_id)
        FROM user_favorites uf
        JOIN know_posts kp ON kp.id = uf.post_id
        WHERE uf.user_id = #{userId}
    </select>

    <select id="countMonthlyNew" resultType="int">
        SELECT COUNT(*) FROM user_favorites
        WHERE user_id = #{userId}
          AND created_at >= NOW() - INTERVAL 30 DAY
    </select>

</mapper>
